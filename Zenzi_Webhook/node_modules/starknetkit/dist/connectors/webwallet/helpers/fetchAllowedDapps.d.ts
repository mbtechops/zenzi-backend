import { constants } from 'starknet';

type AllowedDappsJsonResponse = {
    allowedDapps: string[];
};
export declare const fetchAllowedDapps: (network: constants.NetworkName) => Promise<AllowedDappsJsonResponse>;
export {};
