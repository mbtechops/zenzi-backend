import { SignClientTypes } from '@walletconnect/types';
import { NamespaceAdapter, NamespaceAdapterOptions } from './adapter';

export interface IArgentLoginOptions {
    projectId?: string;
    name?: string;
    description?: string;
    url?: string;
    icons?: string[];
    chainId?: string | number;
    rpcUrl?: string;
    bridgeUrl?: string;
    mobileUrl?: string;
    modalType?: "overlay" | "window";
    walletConnect?: SignClientTypes.Options;
}
export declare const login: <TAdapter extends NamespaceAdapter>({ projectId, chainId, name, description, rpcUrl, bridgeUrl, mobileUrl, modalType, url, icons, walletConnect, }: IArgentLoginOptions, Adapter: new (options: NamespaceAdapterOptions) => TAdapter) => Promise<TAdapter | null>;
